import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'date-time-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
